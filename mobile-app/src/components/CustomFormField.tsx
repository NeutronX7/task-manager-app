import {Text, TextInput} from "react-native";
import {COLORS} from "../constants";
import React from "react";

export default function CustomFormField() {

    return (
        <Text>Aqui ira el form text para login y password</Text>
    )
}